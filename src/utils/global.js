// 下面这个地址是给上传用的地址
const baseUrl = 'http://sxboyu.iok.la:8085'   //JAVA接口地址
const deviceUrl = 'sxboyu.iok.la:82'  //视频API地址
const platUrl = 'http://sxboyu.iok.la:8083'       //项目地址
const userName = 'root'       //设备数据接口
const passWord = 'root'       //设备数据接口
const deptId = ''
const objTitle = {}
export default{
    baseUrl,
    deviceUrl,
    userName,
    passWord,
    deptId,
    platUrl,
    objTitle,
}
